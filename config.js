var host = 'https://fzd.xcloudtech.com:8080';

var config = {

    // 下面的地址配合云端 Demo 工作
    service: {
        host,

        // 上传图片 上传视频
        upFiles: `${host}/mall/problemSubmit`,
    }
};

module.exports = config;
